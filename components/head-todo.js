import { createElement, createButton } from "../utils.js";

export const deleteAllBtn = createButton("Delete All", ["deleteAll"]);

export const inputElement = createElement("input", {
  type: "text",
  placeholder: "Enter to do.."
});

export const addBtn = createButton("Add", ["add"]);







